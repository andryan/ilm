using UnityEngine;
using System.Collections;

public class EmoticonBehaviour : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	public void DestroyEmoticon()
	{
		Destroy (this.gameObject);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
